<section class="content">
  <%= simple_form_for(@order, :url => admin_orders_path, role: 'form', html: { class: 'order_form' } ) do |f| %>
    <%= render 'form' , f: f %>
  <% end %>
  <%= link_to t(:'button.back'), admin_orders_path, class: 'btn btn-default' %>
</section>
