<section class="content">
  <%= simple_form_for(@admin, :url => admin_admin_path(@admin.id), role: 'form', html: { class: 'admin_form' }) do |f| %>
    <%= render 'form' , f: f %>
  <% end %>
  <%= link_to t(:'button.back'), admin_admins_path, class: 'btn btn-default' %>
</section>